/*function muestraMensaje(id){

  $('#eliminar').attr("data-id", id);

  $('#eliminar').click(function(){
    alert($('#eliminar').attr('data-id'));
    /*$.ajax({
      type: 'POST',
      url: Routing.generate('likes'),
      data: ({id: id}),
      async: true,
      dataType: 'json',
      success: function(data){
          
          windows.location.reload();
      }
    });*
  
  });

  $('#cerrar').click(function(){
    $('#eliminar').attr("data-id", "");
    console.log($('#cerrar').attr('data-id'));
    /*$.ajax({
      type: 'POST',
      url: Routing.generate('likes'),
      data: ({id: id}),
      async: true,
      dataType: 'json',
      success: function(data){
          
          windows.location.reload();
      }
    });
  
  });

}*/

